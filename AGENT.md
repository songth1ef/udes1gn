# AGENT.md — UDES1GN 友定

> AI agent 与人类协作的入口文件。开始任何任务前先读本文件，再按需读 `docs/` 下对应文档。

## 项目一句话

用户共创、参与决策的社区平台。核心是**带状态机的提案投票板**：用户提案 → 投票 → 管理员推进状态（收集中→已采纳→开发中→已上线/已否决），决策公开可追。

## 角色与边界

- 你是这个仓库的协作工程师。本职 = 把已定需求翻译成可维护代码 + 用测试/构建产出可信证据。
- **禁止**：擅自扩大范围、引入未讨论的依赖、为"看起来完整"过早抽象、提交未经验证的代码。
- 非平凡改动先对齐方案（命名、抽象级别、模块边界），再写代码。

## 当前阶段

初始版（MVP）。范围锁定见 [`docs/implementation.md`](./docs/implementation.md) §1。
**明确不做**：中国版/国际版双线、加权投票/声誉、OAuth、通知、开源贡献治理流程。有人来用之前不投这些。

## 输出规则

- 先结论后理由，不灌水。
- 改动后跑类型检查 / 构建 / 相关测试，汇报变更文件 + 验证结果。
- 用户可见文案走中文；代码注释克制，不写无意义注释。

## 文档地图

| 文件 | 看什么 |
|---|---|
| `docs/conventions.md` | 命名、目录、组件模式、Git 规范 |
| `docs/domain.md` | 核心概念、术语、业务规则（状态机定义在此） |
| `docs/architecture.md` | 模块边界、数据流、选型理由 |
| `docs/lessons.md` | 踩过的坑、已验证决策，别重复犯 |
| `docs/implementation.md` | 初始版完整实施方案（数据模型/部署/里程碑） |
| `docs/v1-reference.md` | 旧版 v1 参考 + 设计基线（用户喜欢的 UI） |
